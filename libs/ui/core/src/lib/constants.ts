import { InjectionToken } from '@angular/core';

// Tokens
export const PROXY_URL = new InjectionToken('PROXY_URL');
export const CORPORATE_ACCOUNTS_SERVICE = new InjectionToken(
  'CORPORATE_ACCOUNTS_SERVICE',
);
export const FX_RATES_SERVICE = new InjectionToken(
  'FX_RATES_SERVICE',
);